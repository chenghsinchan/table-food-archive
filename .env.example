NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_ALLOWED_EMAILS=chenghsinchan@gmail.com,saulemiezyte@gmail.com

# Web Push (notifications). Generate with: npx web-push generate-vapid-keys
# The PUBLIC key is safe in the browser; the PRIVATE key must stay server-only.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:chenghsinchan@gmail.com

# AI ingredient detection (Gemini 2.5 Flash-Lite). Server-only — never expose
# in the browser or commit a real key. Create one at https://aistudio.google.com/apikey
GEMINI_API_KEY=
